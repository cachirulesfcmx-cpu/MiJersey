export const ATTRIBUTE_REPOSITORY = Symbol('ATTRIBUTE_REPOSITORY');
export const PRODUCT_ATTRIBUTE_REPOSITORY = Symbol('PRODUCT_ATTRIBUTE_REPOSITORY');
export const PRODUCT_QUERY = Symbol('PRODUCT_QUERY');

export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;

/** TTL de la caché de facetas por defecto (sin filtros aplicados). */
export const PUBLIC_CACHE_TTL_SECONDS = 60;
