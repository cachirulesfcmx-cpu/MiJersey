export const PAGE_REPOSITORY = Symbol('PAGE_REPOSITORY');
export const PAGE_VERSION_REPOSITORY = Symbol('PAGE_VERSION_REPOSITORY');

/** TTL de la caché pública de páginas — mismo criterio que `PUBLIC_CACHE_TTL_SECONDS` de Taxonomy (006). */
export const PUBLIC_CACHE_TTL_SECONDS = 60;
