/** Overrides con `!important` para los inputs/botones compartidos de `@mijersey/ui` (no editable directamente: es usado también por apps/admin). */
export const INPUT_OVERRIDE_CLASS =
  '!rounded-xl !border-neutral-200 !py-3 focus-visible:!outline-pop-500';

export const PRIMARY_BUTTON_OVERRIDE_CLASS =
  '!rounded-full !bg-gradient-to-r !from-pop-500 !to-pop-400 !px-6 !py-3 !font-display !uppercase !tracking-wide !text-white !shadow-lg !shadow-pop-600/30 transition-transform hover:!scale-[1.02]';
